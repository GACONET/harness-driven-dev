# CLAUDE.md — <YOUR PROJECT NAME>

## Project

<ONE-PARAGRAPH DESCRIPTION OF YOUR PROJECT.
What does it do? Who uses it? What's the tech stack at a glance?>

The harness connects <YOUR PM TOOL: Linear / Jira / etc.> with <YOUR VCS: GitHub / GitLab / etc.>
through automated enforcement.

## Skills

The agent has <N> skills that connect with the harness:

| Skill | When to use |
|-------|-------------|
| `/create-issue <title>` | Create a new issue in <PM TOOL> with acceptance criteria. |
| `/start-issue <TEAM>-X` | ALWAYS before writing code. Reads the issue, creates branch, moves to In Progress. |
| `/review-pr <TEAM>-X` | Run substantive review of the PR. Posts checklist comment. NEVER auto-approves. |
| `/close-issue <TEAM>-X` | ALWAYS to finish work. Runs 4 gates (tests + CI + PR approval + criteria), posts evidence, moves to Done. |
| `/status` | Check project dashboard: issues, branch, CI status. |

> **Note**: Replace `<TEAM>` with your real team key (e.g., `MYT`, `ENG`, `OPS`).

## Harness Rules

1. **No code without issue**: Before touching code, run `/start-issue`.
2. **Refs, never Closes**: Commit messages MUST contain `Refs <TEAM>-XXX`.
   NEVER use `Closes`, `Fixes`, or `Resolves` — they bypass harness gates.
3. **Mechanical Definition of Done**: Use `/close-issue` to close issues.
   NEVER close manually in <PM TOOL>. The harness runs 4 gates first.
4. **No secrets in code**: gitleaks blocks commits with secrets automatically.
5. **Evidence always**: Every issue closure includes an audit trail comment.
6. **The agent never self-approves**: CI green is NOT enough. Every PR requires
   explicit human approval before `/close-issue` succeeds.
7. **Session hygiene**: When a Claude Code session ends, the Stop hook reports
   unpushed commits, missing Refs, and branches without PRs.

<!-- ADD YOUR TEAM-SPECIFIC RULES HERE. EXAMPLES:
8. **No direct DB queries in routes**: Always go through the service layer.
   Reason: [link to the incident that taught us this]
9. **All new endpoints must have rate-limiting**: Use `@limiter` decorator.
   Reason: [link]
10. **No PRs over 500 LOC**: Split or get exception from tech lead.
    Reason: review quality drops sharply past 500 LOC.
-->

## Commit Message Format

```
<type>: <short description>

<optional body>

Refs <TEAM>-XXX
```

Types: `feat`, `fix`, `docs`, `test`, `chore`, `refactor`

## Scripts

| Script | Purpose |
|--------|---------|
| `scripts/<pm_tool>_client.py` | API client for <PM TOOL> (get, move, comment, list) |
| `scripts/close_issue.sh` | Gate script: tests + CI + PR approval + acceptance criteria |
| `scripts/gates/gate_pr_approval.sh` | Layer 5: verifies PR reviewDecision=APPROVED |
| `scripts/stop_gate.sh` | Layer 6: session hygiene check (Stop hook) |
| `scripts/check_issue_ref.sh` | Commit-msg hook: enforces `Refs <TEAM>-XXX` |
| `scripts/ci_failure_bridge.py` | Auto-creates bug when CI fails |
| `scripts/seed_demo.sh` | Idempotent seed for the 3 live demos |

<!-- ADD YOUR CUSTOM SCRIPTS HERE -->

## Tech Stack

- Frontend: <REACT / VUE / VANILLA / etc.>
- Backend: <PYTHON FASTAPI / NODE EXPRESS / GO / etc.>
- Tests: <PYTEST / JEST / etc.> (target coverage: <X%>)
- Harness scripts: Python 3 (stdlib only) + Bash
- CI: <GITHUB ACTIONS / GITLAB CI / etc.>
- Project management: <LINEAR / JIRA / etc.>

<!-- ADD OTHER STACK ITEMS: databases, queues, infra, etc. -->
